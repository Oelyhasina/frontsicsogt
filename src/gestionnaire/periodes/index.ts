import PeriodeIcon from '@mui/icons-material/Collections';
import { PeriodeCreate } from './periodecreate';
import { PeriodeList } from './periodelist'
import { PeriodeEdit } from './periodeedit';

 const periodes = {
    list: PeriodeList,
    create: PeriodeCreate,
    edit: PeriodeEdit,
    icon: PeriodeIcon,
};

export default periodes;